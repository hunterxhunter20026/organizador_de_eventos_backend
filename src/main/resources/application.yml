spring:
  profiles:
    active: dev

  application:
    name: demo-ds-univalle

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:3050/matricula_db}
    username: ${SPRING_DATASOURCE_USERNAME:matricula_user}
    password: ${SPRING_DATASOURCE_PASSWORD:matricula_pass}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: ${PORT:8085}
  servlet:
    context-path: /demo-ds-univalle

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    try-it-out-enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always